# Optional override when `wx` is not on PATH.
WX_BIN=

# Optional Telegram proxy used by tg-cli/Telethon.
TG_PROXY=

# Optional tg-cli Python package directory if auto-discovery fails.
TG_CLI_SITE_PACKAGES=
